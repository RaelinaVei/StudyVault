<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudyVault | Top Channels</title>
    <style>
        :root {
            --primary: #bc13fe; 
            --text: #ffffff;
            --subtext: #cbd5e1;
            --bg-image: url('https://w0.peakpx.com/wallpaper/700/152/HD-wallpaper-mountains-in-dark-night-background-dark.jpg');
            --card-bg: rgba(255, 255, 255, 0.07); 
            --link-bg: rgba(255, 255, 255, 0.1);
            --glow: 0 0 15px rgba(188, 19, 254, 0.8);
        }

        #starfield {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        body { 
            font-family: 'Segoe UI', sans-serif; 
            color: var(--text); 
            margin: 0; 
            padding: 20px; 
            min-height: 100vh;
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), var(--bg-image);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            overflow-x: hidden;
        }

        .logo-box {
            position: fixed;
            top: 25px;
            left: 25px;
            z-index: 1000;
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }

        .flash-icon {
            font-size: 1.8rem;
            text-shadow: var(--glow);
            filter: drop-shadow(0 0 5px var(--primary));
        }

        .logo-text {
            font-weight: 800;
            letter-spacing: 2px;
            font-size: 1.1rem;
            color: white;
            text-transform: uppercase;
        }

        .container { max-width: 1200px; margin: 0 auto; padding-top: 80px; }

        header { text-align: center; margin-bottom: 50px; }
        
        h1 { 
            font-size: 3rem; 
            color: var(--primary); 
            text-transform: uppercase;
            letter-spacing: 5px;
            text-shadow: var(--glow);
            margin-bottom: 10px;
        }

        .section-header {
            text-align: center;
            color: var(--primary);
            text-transform: uppercase;
            letter-spacing: 4px;
            font-size: 1.5rem;
            margin: 40px 0;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
        }

        .section-header::before, .section-header::after {
            content: "";
            height: 1px;
            width: 100px;
            background: var(--primary);
            box-shadow: var(--glow);
        }

        .channel-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); 
            gap: 20px; 
            margin-bottom: 60px;
        }

        .channel-card { 
            background: var(--card-bg); 
            backdrop-filter: blur(15px);
            padding: 25px; 
            border-radius: 20px; 
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            text-decoration: none;
            color: white;
            transition: 0.3s;
        }

        .channel-card:hover { 
            transform: translateY(-10px); 
            border-color: var(--primary);
            box-shadow: var(--glow);
            background: rgba(188, 19, 254, 0.1);
        }

        .channel-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            filter: drop-shadow(0 0 5px var(--primary));
        }

        .channel-info strong { 
            display: block; 
            font-size: 1.2rem; 
            margin-bottom: 8px; 
            color: var(--primary);
            letter-spacing: 1px;
        }
        
        .channel-info p { 
            font-size: 0.85rem; 
            color: var(--subtext); 
            margin: 0;
            line-height: 1.5;
        }

        .back-nav {
            text-align: center;
            padding-bottom: 50px;
        }

        .back-btn {
            color: white;
            text-decoration: none;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            opacity: 0.6;
            transition: 0.3s;
        }

        .back-btn:hover { opacity: 1; color: var(--primary); text-shadow: var(--glow); }

    </style>
</head>
<body>

<canvas id="starfield"></canvas>

<a href="index.html" class="logo-box">
    <span class="flash-icon">⚡</span>
    <span class="logo-text">StudyVault</span>
</a>

<div class="container">
    <header>
        <h1>Video Archive</h1>
    </header>

    <h2 class="section-header">Best YT Channels</h2>

    <div class="channel-grid">
        <a href="#" class="channel-card">
            <div class="channel-icon">🧪</div>
            <div class="channel-info">
                <strong>Cognito</strong>
                <p>Visual animations for Physics, Chemistry, and Biology.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">📐</div>
            <div class="channel-info">
                <strong>D Math Academy</strong>
                <p>Past paper walkthroughs for Mathematics 4024.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">⚛️</div>
            <div class="channel-info">
                <strong>Physics Mo Ali</strong>
                <p>Mastering O Level Physics concepts and papers.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">🕌</div>
            <div class="channel-info">
                <strong>Sir Muzammil</strong>
                <p>Essential guidance for Islamiat 2058 students.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">🌍</div>
            <div class="channel-info">
                <strong>History Help</strong>
                <p>Detailed analysis of Pakistan Studies history.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">📝</div>
            <div class="channel-info">
                <strong>ZNotes</strong>
                <p>Quick revision and live exam prep sessions.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">📚</div>
            <div class="channel-info">
                <strong>Cambridge Elite</strong>
                <p>Wide variety of resources for multiple subjects.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">💻</div>
            <div class="channel-info">
                <strong>Computer Science</strong>
                <p>Top-tier tutorials for CS 2210 and programming.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">🔬</div>
            <div class="channel-info">
                <strong>FreeSciLessons</strong>
                <p>Straight-to-the-point science revision.</p>
            </div>
        </a>
        <a href="#" class="channel-card">
            <div class="channel-icon">🔥</div>
            <div class="channel-info">
                <strong>CrashCourse</strong>
                <p>High-level overviews of global history and science.</p>
            </div>
        </a>
    </div>

    <div class="back-nav">
        <a href="index.html" class="back-btn">← Back to Dashboard</a>
    </div>
</div>

<script>
    const canvas = document.getElementById('starfield');
    const ctx = canvas.getContext('2d');
    let width, height, stars = [];
    const numStars = 400;

    function initStars() {
        width = window.innerWidth; height = window.innerHeight;
        canvas.width = width; canvas.height = height;
        stars = [];
        for (let i = 0; i < numStars; i++) {
            stars.push({
                x: Math.random() * width - width/2,
                y: Math.random() * height - height/2,
                z: Math.random() * width,
                opacity: Math.random()
            });
        }
    }

    function drawStars() {
        ctx.clearRect(0, 0, width, height);
        for (let i = 0; i < numStars; i++) {
            let star = stars[i];
            star.z -= 1.2;
            if (star.z <= 0) star.z = width;
            const sx = (star.x / star.z) * (width / 2) + width / 2;
            const sy = (star.y / star.z) * (height / 2) + height / 2;
            const size = (1 - star.z / width) * 2.5;
            ctx.beginPath();
            ctx.fillStyle = `rgba(188, 19, 254, ${star.opacity})`;
            ctx.arc(sx, sy, size, 0, Math.PI * 2);
            ctx.fill();
        }
        requestAnimationFrame(drawStars);
    }

    window.addEventListener('resize', initStars);
    initStars(); drawStars();
</script>

</body>
</html>
